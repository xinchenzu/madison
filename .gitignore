output/
*.epub
*.pdf
*.docx
.DS_Store

# generated prompt-suite adapters (rebuild with scripts/build-prompts.mjs)
prompts/**/.build/
instructions/.build/

# generated bytecode + stray backups + build scratch (regenerate; never source)
__pycache__/
*.pyc
*.bak

# one-time build evidence caches (contain student data; archived in full copy)
logs/student-recipe-evidence/

# private per-person strategy/profiles — never committed
people/
brand/private-reflection.md

# vendored upstream app (brandguide.ai, marketmind, scrapers, SurveyAnalysis) —
# quarantined Tier 3: kept on disk + available, out of version control & default agent view
scripts/madison-main/
docs/madison-main/
data/madison-main/
