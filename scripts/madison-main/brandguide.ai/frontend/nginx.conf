server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Proxy API requests if you want Nginx to handle CORS or routing
    # location /api/ {
    #     proxy_pass http://backend:8000/;
    # }
}
